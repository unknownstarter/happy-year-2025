{
  "buildCommand": "if cd flutter; then git pull; else git clone https://github.com/flutter/flutter.git; fi && flutter/bin/flutter build web --release --dart-define=API_URL=https://happy-year-2025-production.up.railway.app",
  "outputDirectory": "build/web",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
} 